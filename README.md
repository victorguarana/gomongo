# Go Mongo

**Go Mongo** is a **simplified version** of MongoDB driver for Golang. It was built to cover implementation needs of the original `mongo-driver`, therefore the project that imports this package **only need to worry about its bussiness logic**.


TODO: Add code examples